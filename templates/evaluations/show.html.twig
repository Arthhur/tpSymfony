{% extends 'base.html.twig' %}

{% block title %}Evaluations{% endblock %}

{% block body %}
    <h1>Evaluations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation.id }}</td>
            </tr>
            <tr>
                <th>Appreciation</th>
                <td>{{ evaluation.appreciation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluations_index') }}">back to list</a>

    <a href="{{ path('evaluations_edit', {'id': evaluation.id}) }}">edit</a>

    {{ include('evaluations/_delete_form.html.twig') }}
{% endblock %}
